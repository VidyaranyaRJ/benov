name: Node.js App Deployment via Rsync to EFS

on:
  push:
    branches: [dev, uat, main]
  workflow_dispatch:
    inputs:
      environment:
        description: "Environment to deploy (dev, uat, prod)"
        required: true
        default: dev
        type: choice
        options: [dev, uat, prod]

env:
  DEPLOY_PATH: /mnt/efs/code/nodejs-app
  AWS_REGION: us-east-2

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set environment name
        run: |
          if [[ "${{ github.event_name }}" == "workflow_dispatch" ]]; then
            echo "ENVIRONMENT=${{ github.event.inputs.environment }}" >> $GITHUB_ENV
          elif [[ "${GITHUB_REF_NAME}" == "main" ]]; then
            echo "ENVIRONMENT=prod" >> $GITHUB_ENV
          else
            echo "ENVIRONMENT=${GITHUB_REF_NAME}" >> $GITHUB_ENV
          fi
          echo "🎯 Deploying to environment: $ENVIRONMENT"

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y unzip curl
          curl "https://awscli.amazonaws.com/awscli-exe-linux-x86_64.zip" -o "awscliv2.zip"
          unzip -o awscliv2.zip
          sudo ./aws/install --update
          aws --version

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Setup SSH key
        run: |
          echo "${{ secrets.EC2_SSH_KEY }}" > ec2_key.pem
          chmod 600 ec2_key.pem

      - name: Get EC2 instances for deployment
        id: get_instances
        run: |
          echo "🔍 Finding EC2 instances for environment: $ENVIRONMENT"
          
          # Get instances in JSON format for better parsing
          INSTANCES_JSON=$(aws ec2 describe-instances \
            --region "$AWS_REGION" \
            --filters "Name=tag:Environment,Values=${ENVIRONMENT}" \
                      "Name=instance-state-name,Values=running" \
            --query 'Reservations[*].Instances[*].[InstanceId,PublicIpAddress,Tags[?Key==`Name`].Value|[0]]' \
            --output json)
          
          echo "Raw JSON output:"
          echo "$INSTANCES_JSON"
          
          # Extract public IPs, filtering out null values
          PUBLIC_IPS=$(echo "$INSTANCES_JSON" | jq -r '.[][] | select(.[1] != null) | .[1]' | tr '\n' ' ')
          
          if [[ -z "$PUBLIC_IPS" || "$PUBLIC_IPS" == " " ]]; then
            echo "❌ No running EC2 instances with public IPs found for environment: $ENVIRONMENT"
            echo "Available instances:"
            echo "$INSTANCES_JSON" | jq -r '.[][] | "\(.[0]) - \(.[2] // "No Name") - Public IP: \(.[1] // "None")"'
            exit 1
          fi
          
          # Clean up the IPs string
          PUBLIC_IPS=$(echo "$PUBLIC_IPS" | xargs)
          
          echo "EC2_INSTANCE_IPS=$PUBLIC_IPS" >> "$GITHUB_ENV"
          echo "📋 Found instances with public IPs: $PUBLIC_IPS"
          
          # Count instances
          IP_COUNT=$(echo "$PUBLIC_IPS" | wc -w)
          echo "📊 Total instances: $IP_COUNT"

      - name: Test SSH connectivity
        run: |
          echo "🔧 Testing SSH connectivity..."
          SUCCESS_COUNT=0
          TOTAL_COUNT=0
          
          for ip in $EC2_INSTANCE_IPS; do
            TOTAL_COUNT=$((TOTAL_COUNT + 1))
            echo "Testing SSH to $ip..."
            
            if ssh -i ec2_key.pem -o StrictHostKeyChecking=no -o ConnectTimeout=15 -o BatchMode=yes ec2-user@$ip "echo 'Connected to $ip'" 2>/dev/null; then
              echo "✅ SSH connection successful to $ip"
              SUCCESS_COUNT=$((SUCCESS_COUNT + 1))
            else
              echo "❌ SSH connection failed to $ip"
              echo "Retrying with verbose output..."
              ssh -i ec2_key.pem -o StrictHostKeyChecking=no -o ConnectTimeout=15 -vvv ec2-user@$ip "echo 'test'" 2>&1 | head -20
            fi
          done
          
          if [[ $SUCCESS_COUNT -eq 0 ]]; then
            echo "❌ No SSH connections successful. Aborting deployment."
            exit 1
          elif [[ $SUCCESS_COUNT -lt $TOTAL_COUNT ]]; then
            echo "⚠️  Only $SUCCESS_COUNT out of $TOTAL_COUNT instances accessible via SSH"
            echo "Continuing with accessible instances..."
          else
            echo "✅ All $TOTAL_COUNT instances accessible via SSH"
          fi

      - name: Deploy application files
        run: |
          echo "🚀 Starting deployment..."
          SUCCESS_COUNT=0
          
          for ip in $EC2_INSTANCE_IPS; do
            echo "📁 Deploying to $ip..."
            
            # Test SSH first
            if ! ssh -i ec2_key.pem -o StrictHostKeyChecking=no -o ConnectTimeout=10 -o BatchMode=yes ec2-user@$ip "echo 'test'" >/dev/null 2>&1; then
              echo "❌ Skipping $ip - SSH not accessible"
              continue
            fi
            
            # Create deployment directory if it doesn't exist
            ssh -i ec2_key.pem -o StrictHostKeyChecking=no ec2-user@$ip "sudo mkdir -p $DEPLOY_PATH && sudo chown ec2-user:ec2-user $DEPLOY_PATH"
            
            # Sync files
            if rsync -avz --delete \
              -e "ssh -i ec2_key.pem -o StrictHostKeyChecking=no" \
              --exclude='node_modules/' \
              --exclude='.git/' \
              --exclude='*.log' \
              --exclude='.env' \
              ./Nodejs/ ec2-user@$ip:$DEPLOY_PATH/; then
              echo "✅ File sync completed for $ip"
              SUCCESS_COUNT=$((SUCCESS_COUNT + 1))
            else
              echo "❌ File sync failed for $ip"
            fi
          done
          
          if [[ $SUCCESS_COUNT -eq 0 ]]; then
            echo "❌ No deployments successful"
            exit 1
          else
            echo "✅ Deployed successfully to $SUCCESS_COUNT instances"
          fi

      - name: Restart application services
        run: |
          echo "🔄 Restarting application services..."
          RESTART_SUCCESS=0
          RESTART_TOTAL=0
          
          for ip in $EC2_INSTANCE_IPS; do
            echo "🚀 Restarting services on $ip..."
            RESTART_TOTAL=$((RESTART_TOTAL + 1))
            
            # Test SSH first
            if ! ssh -i ec2_key.pem -o StrictHostKeyChecking=no -o ConnectTimeout=10 -o BatchMode=yes ec2-user@$ip "echo 'test'" >/dev/null 2>&1; then
              echo "❌ Skipping $ip - SSH not accessible"
              continue
            fi
            
            # Check if PM2 script exists and run it
            if ssh -i ec2_key.pem -o StrictHostKeyChecking=no ec2-user@$ip "test -f $DEPLOY_PATH/scripts/pm2-reset-restart.sh"; then
              echo "📋 Running PM2 reset script on $ip..."
              
              # Run the PM2 script and capture its exit code
              if ssh -i ec2_key.pem -o StrictHostKeyChecking=no ec2-user@$ip "cd $DEPLOY_PATH && bash scripts/pm2-reset-restart.sh"; then
                echo "✅ PM2 restart completed successfully for $ip"
                RESTART_SUCCESS=$((RESTART_SUCCESS + 1))
              else
                SCRIPT_EXIT_CODE=$?
                echo "❌ PM2 restart script failed for $ip with exit code: $SCRIPT_EXIT_CODE"
                echo "🔍 Checking PM2 status..."
                ssh -i ec2_key.pem -o StrictHostKeyChecking=no ec2-user@$ip "pm2 list" || echo "Could not get PM2 status"
                echo "🔍 Checking if app is running manually..."
                ssh -i ec2_key.pem -o StrictHostKeyChecking=no ec2-user@$ip "ps aux | grep node" || echo "No node processes found"
              fi
            else
              echo "⚠️  PM2 script not found on $ip, attempting manual restart..."
              if ssh -i ec2_key.pem -o StrictHostKeyChecking=no ec2-user@$ip "cd $DEPLOY_PATH && (pm2 restart all || pm2 start ecosystem.config.js || pm2 start app.js --name nodejs-app)"; then
                echo "✅ Manual restart completed for $ip"
                RESTART_SUCCESS=$((RESTART_SUCCESS + 1))
              else
                echo "❌ Manual restart failed for $ip"
              fi
            fi
          done
          
          echo "📊 Restart Summary: $RESTART_SUCCESS/$RESTART_TOTAL instances restarted successfully"
          
          # Don't fail the workflow if at least one instance was restarted successfully
          if [[ $RESTART_SUCCESS -gt 0 ]]; then
            echo "✅ At least one instance restarted successfully"
          else
            echo "❌ No instances restarted successfully"
            exit 1
          fi

      - name: Verify deployment
        run: |
          echo "🔍 Verifying deployment..."
          VERIFY_SUCCESS=0
          
          for ip in $EC2_INSTANCE_IPS; do
            echo "Checking $ip..."
            
            if ssh -i ec2_key.pem -o StrictHostKeyChecking=no -o ConnectTimeout=10 ec2-user@$ip "pm2 list" 2>/dev/null; then
              echo "✅ PM2 processes verified on $ip"
              VERIFY_SUCCESS=$((VERIFY_SUCCESS + 1))
              
              # Check if the app is actually running
              if ssh -i ec2_key.pem -o StrictHostKeyChecking=no ec2-user@$ip "pm2 list | grep -q 'online'"; then
                echo "✅ Application is running on $ip"
              else
                echo "⚠️  PM2 is running but no online processes on $ip"
              fi
              
              # Check if port 3000 is listening
              if ssh -i ec2_key.pem -o StrictHostKeyChecking=no ec2-user@$ip "netstat -tlnp | grep ':3000 ' || ss -tlnp | grep ':3000 '"; then
                echo "✅ Port 3000 is listening on $ip"
              else
                echo "⚠️  Port 3000 is not listening on $ip"
              fi
            else
              echo "⚠️  Could not verify PM2 status on $ip"
            fi
          done
          
          echo "📊 Deployment Summary:"
          echo "- Files deployed successfully to all instances"
          echo "- PM2 restart attempted on all instances"
          echo "- Verification completed for $VERIFY_SUCCESS instances"
          echo "✅ Deployment process completed"

      - name: Cleanup
        if: always()
        run: |
          echo "🧹 Cleaning up..."
          rm -f ec2_key.pem
          echo "✅ Cleanup completed"